import uniqueHtmlId from 'utils/uniqueHtmlId';

/**
 * id для указания в html
 */
export const MOD_HTML_ID = {
  CONTACTS: uniqueHtmlId(),
  GALLERY: uniqueHtmlId(),
  HEADER: uniqueHtmlId(),
  MUSIC: uniqueHtmlId(),
  NEWS: uniqueHtmlId(),
  VIDEOS: uniqueHtmlId(),
};
